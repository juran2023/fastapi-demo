# 并发选型结论

1. **IO 密集型用 async 或线程池** — 两者都能并发等待 IO，async 更轻量（单线程几万协程），线程池兼容同步库。
2. **CPU 密集型用进程池** — 线程池受 GIL 限制，多线程跑纯计算约等于串行；进程池绕过 GIL，真正利用多核并行。
3. **async 里不要跑 CPU 密集代码** — 协程只在 `await` 处切换，纯计算会阻塞整个事件循环，导致所有协程饿死。
